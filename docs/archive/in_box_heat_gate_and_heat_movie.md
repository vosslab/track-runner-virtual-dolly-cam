# Archived plan: Blob Walk v2 heat diagnostics

`tools/blob_walk_v2` was removed. The current application computes residual
heat inside `track_runner/` without this offline diagnostic workflow. This
file remains only as a stable destination for historical references; it
authorizes no work, contains no validation gates, and has no private-media
dependency.
